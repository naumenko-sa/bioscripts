#!/bin/bash

#PBS -l walltime=2:00:00,nodes=1:ppn=1
#PBS -d .
#PBS -joe
#PBS -l vmem=10g

#usage sample=sample_name, sample_name.sra should be in the current directory

module load sratoolkit

fastq-dump -I --gzip --split-files $sample.sra

mv ${sample}_1.fastq.gz ${sample}_1.fq.gz
mv ${sample}_2.fastq.gz ${sample}_2.fq.gz
